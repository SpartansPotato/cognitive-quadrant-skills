# 在 WorkBuddy 中安装

1. 从 `releases/` 下载其中一个通用 ZIP；
2. 打开 WorkBuddy 的 Skill 管理；
3. 选择上传、导入或添加本地 Skill；
4. 选择 ZIP 并启用；
5. 使用 Skill README 中的测试提示词进行验证。

工具包包含 WorkBuddy 所需的 `SKILL.md` 和 `manifest.yaml`，同时包含参考规则和模板资源，默认不要求用户配置外部 API Key。

不同 WorkBuddy 版本或企业策略中的菜单名称可能略有差异。若个人上传功能被关闭，请由管理员统一上传并下发。
